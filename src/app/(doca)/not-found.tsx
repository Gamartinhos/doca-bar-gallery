import { NotFoundContent } from "@/components/not-found-content";

/** 404 disparado por `notFound()` dentro das rotas do Doca. */
export default function DocaNotFound() {
  return <NotFoundContent />;
}
